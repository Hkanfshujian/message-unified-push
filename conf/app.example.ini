[app]
JwtSecret = ops-message-unified-push
RuntimeRootPath = runtime/
LogLevel = INFO

; init table data, first run set enable
InitData = enable

[server]
; debug or release
RunMode = release
HttpPort = 8000
ReadTimeout = 60
WriteTimeout = 60
; use embed html static file
; EmbedHtml = disable
; URL prefix for sub-path deployment (e.g., /ops-message-unified-push)
; UrlPrefix = /ops-message-unified-push

[database]
Type = mysql
User = message_user
Password = K3mX0t2lG
Host = 192.168.26.11
Name = message
Port = 3307
TablePrefix = message_

; ssl enable, value: [false | true]
Ssl = true

; SqlDebug = enable
